name: 'Terragrunt CI/CD'
# Trigger jumpstart: 2026-04-23

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:
    inputs:
      target_region:
        description: 'Target AWS Region (Overrides vars.AWS_REGION if provided)'
        required: false
        type: string

permissions:
  id-token: write
  contents: read
  actions: read
  pull-requests: write
  security-events: write

env:
  AWS_REGION: ${{ github.event.inputs.target_region || vars.AWS_REGION }}
  # Use modules from this checkout instead of the pinned release tags (module_versions in env.hcl).
  # Needed until every module has a release tag at the iac-modules-repo path (PLAN 1.4), and later for
  # PRs that change iac-modules-repo/** so module changes are tested before release.
  IAC_MODULES_LOCAL: '1'

jobs:
  # ─────────────────────────────────────────────────────────────
  # Gate 1: High-Speed Static Analysis (TFLint/Checkov)
  # ─────────────────────────────────────────────────────────────
  static-analysis:
    name: '🔍 Static Analysis (TFLint/Checkov)'
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/ok-karthik/infrastructure-toolchain:latest
      options: --user root
      credentials:
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}
    timeout-minutes: 10
    permissions:
      id-token: write
      contents: read
      pull-requests: write
      security-events: write
    steps:
      - name: "Git Checkout"
        uses: actions/checkout@v6

      - name: "Setup Platform"
        uses: ./.github/actions/setup-platform

      - name: "Static Analysis"
        uses: ./.github/actions/static-analysis

      - name: Upload Trivy SARIF results
        uses: github/codeql-action/upload-sarif@v4
        if: always()
        with:
          sarif_file: 'trivy-results.sarif'
          category: 'static-analysis'



  # ─────────────────────────────────────────────────────────────
  # Gate 2-4: Multi-Environment Orchestration (Reusable)
  # ─────────────────────────────────────────────────────────────
  dev-stack:
    name: 'Dev'
    uses: ./.github/workflows/reusable-terragrunt.yml
    with:
      environment: 'dev'
      working_directory: 'workloads-live-repo/dev'
      aws_plan_role_arn: ${{ vars.AWS_DEV_PLAN_ROLE_ARN }}
    secrets:
      INFRACOST_API_KEY: ${{ secrets.INFRACOST_API_KEY }}

  prod-stack:
    name: 'Prod'
    uses: ./.github/workflows/reusable-terragrunt.yml
    with:
      environment: 'prod'
      working_directory: 'workloads-live-repo/prod'
      aws_plan_role_arn: ${{ vars.AWS_PROD_PLAN_ROLE_ARN }}
    secrets:
      INFRACOST_API_KEY: ${{ secrets.INFRACOST_API_KEY }}

  # ─────────────────────────────────────────────────────────────
  # Phase 5: Deployment (Compliance-First Promotion)
  # ─────────────────────────────────────────────────────────────
  apply-dev:
    name: '🚀 Apply: Dev'
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    needs: [dev-stack]
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/ok-karthik/infrastructure-toolchain:latest
      options: --user root
      credentials:
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}
    timeout-minutes: 30
    environment: dev
    steps:
      - name: "Git Checkout"
        uses: actions/checkout@v6

      - name: "Setup Platform"
        uses: ./.github/actions/setup-platform
        with:
          aws_region: ${{ env.AWS_REGION }}
          aws_role_arn: ${{ vars.AWS_DEV_APPLY_ROLE_ARN }}

      - name: "Terragrunt Apply"
        run: terragrunt run --all apply --non-interactive -auto-approve
        working-directory: workloads-live-repo/dev

  apply-prod:
    name: '🚀 Apply: Prod'
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    needs: [apply-dev, prod-stack]
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/ok-karthik/infrastructure-toolchain:latest
      options: --user root
      credentials:
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}
    timeout-minutes: 30
    environment: prod
    steps:
      - name: "Git Checkout"
        uses: actions/checkout@v6

      - name: "Setup Platform"
        uses: ./.github/actions/setup-platform
        with:
          aws_region: ${{ env.AWS_REGION }}
          aws_role_arn: ${{ vars.AWS_PROD_APPLY_ROLE_ARN }}

      - name: "Terragrunt Apply"
        run: terragrunt run --all apply --non-interactive -auto-approve
        working-directory: workloads-live-repo/prod
